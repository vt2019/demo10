apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/jenkinsx-pw1/demo10:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/jenkinsx-pw1/demo10:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: demo10
        chartPath: charts/demo10
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/jenkinsx-pw1/demo10"
          image.tag: "{{.DIGEST_HEX}}"
